apiVersion: v1
kind: Service
metadata:
  name: qrcode-backend
  namespace: qrcode
  labels:
    app: qrcode-backend
spec:
  selector:
    app: qrcode-backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: backend-port
  type: ClusterIP
