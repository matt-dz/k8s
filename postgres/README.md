# Postgres

Configs for Postgres database. Volumes are setup to use NFS, but feel free to change to desired mode of storage. Ensure the directory is owned by the postgres user with the command `chown -R 999:999 /path/to/volume/`.
