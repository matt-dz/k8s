apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dss-ingress
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - dss.deguzman.cloud
    secretName: dsa-tls
  rules:
  - host: dss.deguzman.cloud
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: dsa-frontend
              port:
                number: 80
