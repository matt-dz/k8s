apiVersion: v1
kind: Service
metadata:
  name: kube-deploy
  namespace: kube-deploy
  labels:
    app: kube-deploy
spec:
  selector:
    app: kube-deploy
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
